# MCP C# SDK

This directory contains the C# SDK for the Model Context Protocol (MCP).

## Installation

Install the NuGet package:

```bash
dotnet add package MCP.CSharp.SDK
```

## Usage

```csharp
using MCP.CSharp.SDK;

// Example usage
var client = new MCPClient();
client.Connect();
```
